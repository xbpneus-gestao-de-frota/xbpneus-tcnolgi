from django.apps import AppConfig


class MultasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'backend.transportador.multas'
    verbose_name = 'Gestão de Multas e Infrações'
